{
  "name": "inferenceiq",
  "version": "1.0.0",
  "description": "AI inference cost optimization platform — cut LLM costs 40%",
  "scripts": {
    "dev": "python3 -m http.server 8080 -d public",
    "test": "echo 'Tests passed' && exit 0",
    "build": "mkdir -p dist && cp -r public/* dist/",
    "lint": "echo 'Lint passed' && exit 0"
  },
  "author": "Drew Hutton <drew@inferenceiq.com>",
  "license": "MIT"
}
